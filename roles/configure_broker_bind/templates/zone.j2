$ORIGIN .
$TTL 1 ; 1 seconds (for testing only)
{{ domainname }}               IN SOA  ns1.{{ domainname }}. hostmaster.{{ domainname }}. (
                                2011112904 ; serial
                                60         ; refresh (1 minute)
                                15         ; retry (15 seconds)
                                1800       ; expire (30 minutes)
                                10         ; minimum (10 seconds)
                                )
                        NS      ns1.{{ domainname }}.
                        MX      10 mail.{{ domainname }}.
$ORIGIN {{ domainname }}.
ns1                     A       127.0.0.1
